// Root build file — intentionally empty. Per-module configuration lives in
// gitsema-core/build.gradle.kts. Kept minimal so adding a second module later
// (e.g. a JVM-only desktop tool, per docs/design/kotlin-port.md §10 Decision A)
// doesn't require restructuring this file.
